{
  "name": "room-reservation-system",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc --noEmit && eslint src/",
    "db:migrate": "npx prisma migrate deploy",
    "db:generate": "npx prisma generate",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:test": "docker compose up -d --build && sleep 5 && ./scripts/smoke-test.sh && docker compose down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Room reservation system - semester project",
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/helmet": "^13.0.2",
    "@fastify/rate-limit": "^10.3.0",
    "@prisma/client": "^6.19.2",
    "dotenv": "^17.3.1",
    "fastify": "^5.8.2",
    "prisma": "^6.19.2"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.3.5",
    "@vitest/coverage-v8": "^4.0.18",
    "c8": "^11.0.0",
    "eslint": "^10.0.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1",
    "vitest": "^4.0.18"
  }
}
